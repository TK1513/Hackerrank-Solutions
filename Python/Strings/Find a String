def count_substring(string, sub_string):
    count = 0
    i = 0
    while i < len(string):
        if string.find(sub_string,i)>= 0:
            i = string.find(sub_string,i)+1
            count += 1
        else:break
    return count
